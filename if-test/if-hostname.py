#!/usr/bin/env python3
hostname = 'MTG'
hostname = (input('Enter your hostname: '))

if hostname.upper() == 'MTG': # this allows any form of MTG to be accepted, MTG, mtg, MTg, mtG..
    print('The hostname was correct')
print('Exiting the script')
     

